---
// =============================================================================
// LAYOUT BASE - Estok Inventory System
// Estructura HTML principal con Tailwind CSS y meta tags
// =============================================================================

// Importar estilos globales (Astro los procesa y los pone en /assets/ con hash)
import '../styles/global.css';
import ChatModal from '../components/ChatModal.astro';

export interface Props {
  title?: string;
  description?: string;
}

const { title = 'Estok - Sistema de Inventario', description = 'Sistema de gestión de inventario personal con herencia multi-tabla' } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#8B4513" />

    <title>{title}</title>

    <!-- Favicon (cache buster v2) -->
    <link rel="icon" type="image/png" href="/favicon.png?v=2" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg?v=2" />
    <link rel="mask-icon" href="/favicon.svg?v=2" color="#8B4513" />
    <link rel="shortcut icon" href="/favicon.png?v=2" type="image/png" />

    <!-- Fuente Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />

    <!-- Cropper.js CSS + JS (para editor de fotos) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.2/cropper.min.css" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.2/cropper.min.js" defer></script>

    <!-- PWA: Manifest -->
    <link rel="manifest" href="/manifest.json" />
    <meta name="mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-title" content="Estok" />
    <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />

  </head>
  <body>
    <!-- Barra de navegación -->
    <nav class="bg-white shadow-sm border-b border-gray-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <!-- Logo y marca -->
          <div class="flex items-center">
            <a href="/" class="flex items-center space-x-2">
              <img src="/favicon.png" alt="Estok" class="h-16 w-16 object-cover" />
              <span class="text-2xl font-bold text-blue-700">Estok</span>
              <span class="text-sm text-gray-500 hidden sm:inline">| Inventario</span>
            </a>
          </div>

          <!-- Navegación principal -->
          <div class="hidden md:flex items-center space-x-6">
            <a href="/" class="text-gray-600 hover:text-blue-700 transition-base font-medium">
              Dashboard
            </a>
            <a href="/objetos" class="text-gray-600 hover:text-blue-700 transition-base font-medium">
              Objetos
            </a>
            <a href="/ubicaciones" class="text-gray-600 hover:text-blue-700 transition-base font-medium">
              Ubicaciones
            </a>
            <a href="/contenedores" class="text-gray-600 hover:text-blue-700 transition-base font-medium">
              Contenedores
            </a>
          </div>

          <!-- Estok activo + Invitar -->
          <div id="estokSection" class="hidden md:flex items-center space-x-3">
            <div id="estokInfo" class="flex items-center gap-2 px-3 py-1.5 bg-blue-50 rounded-lg border border-blue-100">
              <svg class="w-4 h-4 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
              </svg>
              <span id="estokNombre" class="text-sm font-semibold text-blue-800"></span>
              <span id="estokRoleBadge" class="text-xs px-1.5 py-0.5 rounded-full bg-blue-200 text-blue-800 font-medium"></span>
            </div>
            <button id="invitarBtn"
              class="hidden px-3 py-1.5 text-sm font-medium text-white bg-green-600 hover:bg-green-700 rounded-lg transition-base shadow-sm"
              title="Invitar miembros al Estok">
              <svg class="w-4 h-4 inline-block mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
              </svg>
              Invitar
            </button>
          </div>

          <!-- Acciones / Usuario -->
          <div class="flex items-center space-x-3">
            <!-- Botón Instalar App (PWA) -->
            <button id="installAppBtn"
              class="p-2 rounded-lg text-blue-700 hover:text-blue-900 hover:bg-blue-50 transition-base relative group"
              title="Instalar Estok en tu dispositivo">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
              </svg>
              <span class="absolute -top-8 left-1/2 -translate-x-1/2 bg-gray-800 text-white text-xs px-2 py-1 rounded opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap pointer-events-none">
                Descargar App
              </span>
            </button>
            <!-- Dark Mode Toggle -->
            <button id="darkModeToggle" class="p-2 rounded-lg text-gray-500 hover:text-gray-700 hover:bg-gray-100 transition-base" title="Cambiar tema">
              <svg id="sunIcon" class="w-5 h-5 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
              </svg>
              <svg id="moonIcon" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
              </svg>
            </button>
            <div id="authSection" class="hidden flex items-center gap-3">
              <span id="userName" class="text-sm text-gray-600 font-medium"></span>
              <!-- Botón Chat (sobre amarillo) en la barra -->
              <button id="chatNavBtn"
                class="p-2 rounded-lg text-yellow-600 hover:text-yellow-800 hover:bg-yellow-50 transition-base relative"
                title="Chat del Estok">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
                <span id="chatNavBadge" class="hidden absolute top-0.5 right-0.5 w-2.5 h-2.5 bg-red-500 rounded-full"></span>
              </button>
              <button
                id="logoutBtn"
                class="text-sm text-red-600 hover:text-red-700 transition-base font-medium"
              >
                Cerrar Sesión
              </button>
            </div>
            <a id="loginLink" href="/login" class="text-sm text-gray-600 hover:text-blue-700 transition-base">
              Iniciar Sesión
            </a>
          </div>

        </div>
      </div>
    </nav>

    <!-- Toast Container Global -->
    <div id="toastContainer" class="fixed top-4 right-4 z-[9999] flex flex-col gap-2 pointer-events-none"></div>

    <!-- Chat Modal -->
    <ChatModal />

    <!-- Contenido principal -->
    <main class="min-h-screen">
      <slot />
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t border-gray-200 mt-auto">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
        <div class="flex flex-col md:flex-row justify-between items-center space-y-4 md:space-y-0">
          <p class="text-sm text-gray-500">
            &copy; {new Date().getFullYear()} Estok - Sistema de Inventario Personal
          </p>
          <p class="text-sm text-gray-400">
            Construido con Astro + Tailwind CSS
          </p>
        </div>
      </div>
    </footer>
  </body>
</html>

<script>
  import { getCachedUser, getEstokActivoId, generarCodigoInvitacion, fetchRoles as fetchRolesFromAuth } from '../services/auth';

  // ===========================================================================
  // SISTEMA DE TOASTS GLOBAL
  // ===========================================================================

  const toastContainer = document.getElementById('toastContainer')!;
  let toastCount = 0;

  const TOAST_ICONS: Record<string, string> = {
    success: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>`,
    error: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>`,
    info: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>`,
    warning: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z" /></svg>`,
  };

  const TOAST_COLORS: Record<string, string> = {
    success: 'bg-green-600 text-white',
    error: 'bg-red-600 text-white',
    info: 'bg-blue-600 text-white',
    warning: 'bg-amber-500 text-white',
  };

  function showToast(message: string, type: string = 'info', duration: number = 4000): string {
    const id = `toast-${++toastCount}`;
    const toast = document.createElement('div');
    toast.id = id;
    toast.className = `pointer-events-auto flex items-center gap-3 px-4 py-3 rounded-lg shadow-lg ${TOAST_COLORS[type] || TOAST_COLORS.info} transform translate-x-full opacity-0 transition-all duration-300 ease-out max-w-sm`;
    toast.innerHTML = `
      <span class="flex-shrink-0">${TOAST_ICONS[type] || TOAST_ICONS.info}</span>
      <span class="flex-1 text-sm font-medium">${message}</span>
      <button onclick="document.getElementById('${id}')?.remove()" class="flex-shrink-0 opacity-70 hover:opacity-100">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    `;
    toastContainer.appendChild(toast);

    requestAnimationFrame(() => {
      toast.classList.remove('translate-x-full', 'opacity-0');
    });

    if (duration > 0) {
      setTimeout(() => {
        toast.classList.add('translate-x-full', 'opacity-0');
        setTimeout(() => toast.remove(), 300);
      }, duration);
    }

    return id;
  }

  // Exponer globalmente
  (window as any).showToast = showToast;
  (window as any).showSuccess = (msg: string) => showToast(msg, 'success');
  (window as any).showError = (msg: string) => showToast(msg, 'error');
  (window as any).showInfo = (msg: string) => showToast(msg, 'info');
  (window as any).showWarning = (msg: string) => showToast(msg, 'warning');

  // ===========================================================================
  // CONTROL DE AUTENTICACIÓN EN LA NAVEGACIÓN
  // ===========================================================================

  const authSection = document.getElementById('authSection')!;
  const loginLink = document.getElementById('loginLink')!;
  const userName = document.getElementById('userName')!;
  const logoutBtn = document.getElementById('logoutBtn')!;

  // Elementos del Estok activo
  const estokSection = document.getElementById('estokSection')!;
  const estokNombre = document.getElementById('estokNombre')!;
  const estokRoleBadge = document.getElementById('estokRoleBadge')!;
  const invitarBtn = document.getElementById('invitarBtn')!;

  function updateEstokUI() {
    const user = getCachedUser();
    const estokActivoId = getEstokActivoId();

    if (!user || !estokActivoId) {
      estokSection.classList.add('hidden');
      return;
    }

    const estokActivo = user.estoks?.find((e: any) => e.id === estokActivoId);

    if (estokActivo) {
      estokSection.classList.remove('hidden');
      estokNombre.textContent = estokActivo.nombre;
      estokRoleBadge.textContent = estokActivo.role || '';

      // Mostrar botón Invitar solo si es Admin
      if (estokActivo.role === 'Admin') {
        invitarBtn.classList.remove('hidden');
      } else {
        invitarBtn.classList.add('hidden');
      }
    } else {
      estokSection.classList.add('hidden');
    }
  }

  function updateAuthUI() {
    const token = localStorage.getItem('estok_access_token');
    const userData = localStorage.getItem('estok_user');

    if (token && userData) {
      try {
        const user = JSON.parse(userData);
        authSection.classList.remove('hidden');
        loginLink.classList.add('hidden');
        userName.textContent = user.first_name
          ? `${user.first_name} ${user.last_name || ''}`
          : user.username;
        updateEstokUI();
      } catch {
        authSection.classList.add('hidden');
        loginLink.classList.remove('hidden');
        estokSection.classList.add('hidden');
      }
    } else {
      authSection.classList.add('hidden');
      loginLink.classList.remove('hidden');
      estokSection.classList.add('hidden');
    }
  }

  logoutBtn?.addEventListener('click', () => {
    localStorage.removeItem('estok_access_token');
    localStorage.removeItem('estok_refresh_token');
    localStorage.removeItem('estok_user');
    window.location.href = '/login';
  });

  // Actualizar UI al cargar la página
  updateAuthUI();

  // ===========================================================================
  // MODAL DE INVITACIÓN
  // ===========================================================================

  function crearModalInvitacion() {
    // Evitar duplicados
    if (document.getElementById('invitarModal')) return;

    const overlay = document.createElement('div');
    overlay.id = 'invitarModal';
    overlay.className = 'fixed inset-0 z-[9998] flex items-center justify-center bg-black/50 backdrop-blur-sm';
    overlay.innerHTML = `
      <div class="bg-white rounded-2xl shadow-2xl w-full max-w-lg mx-4 overflow-hidden transform transition-all">
        <!-- Header -->
        <div class="flex items-center justify-between px-6 py-4 border-b border-gray-200">
          <h3 class="text-lg font-bold text-gray-900">Invitar miembros</h3>
          <button id="cerrarModalBtn" class="p-1 rounded-lg text-gray-400 hover:text-gray-600 hover:bg-gray-100 transition-base">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>

        <!-- Tabs -->
        <div class="flex border-b border-gray-200">
          <button id="tabCodigo" class="flex-1 px-4 py-3 text-sm font-medium text-blue-700 border-b-2 border-blue-700 transition-base">
            🔑 Generar código
          </button>
          <button id="tabEmail" class="flex-1 px-4 py-3 text-sm font-medium text-gray-500 hover:text-gray-700 transition-base">
            📧 Enviar por email
          </button>
        </div>

        <!-- Contenido: Generar código -->
        <div id="panelCodigo" class="p-6">
          <div class="space-y-4">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Rol a asignar</label>
              <select id="rolSelector" class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                <option value="">Cargando roles...</option>
              </select>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Usos máximos <span class="text-gray-400 font-normal">(opcional, 0 = sin límite)</span></label>
              <input type="number" id="usosMaximos" min="0" value="0" class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500" />
            </div>
            <button id="generarCodigoBtn"
              class="w-full px-4 py-2.5 text-sm font-semibold text-white bg-blue-700 hover:bg-blue-800 rounded-lg transition-base disabled:opacity-50 disabled:cursor-not-allowed shadow-sm">
              Generar código
            </button>
            <div id="codigoGenerado" class="hidden">
              <div class="p-4 bg-green-50 border border-green-200 rounded-lg">
                <p class="text-xs text-green-700 font-medium mb-2">✅ Código generado exitosamente</p>
                <div class="flex items-center gap-2">
                  <code id="codigoTexto" class="flex-1 px-3 py-2 bg-white border border-green-300 rounded-lg text-lg font-mono font-bold text-green-800 text-center tracking-wider select-all"></code>
                  <button id="copiarCodigoBtn"
                    class="px-3 py-2 text-sm font-medium text-white bg-green-600 hover:bg-green-700 rounded-lg transition-base shadow-sm whitespace-nowrap">
                    Copiar
                  </button>
                </div>
              </div>
            </div>
            <div id="codigoError" class="hidden p-3 bg-red-50 border border-red-200 rounded-lg text-sm text-red-700"></div>
          </div>
        </div>

        <!-- Contenido: Enviar por email (deshabilitado) -->
        <div id="panelEmail" class="p-6 hidden">
          <div class="p-6 bg-gray-50 border border-gray-200 rounded-lg text-center">
            <p class="text-3xl mb-3">🚧</p>
            <p class="text-sm font-medium text-gray-700">Envío por email</p>
            <p class="text-xs text-gray-500 mt-1">Próximamente disponible</p>
          </div>
        </div>
      </div>
    `;

    document.body.appendChild(overlay);

    // Lógica de tabs
    const tabCodigo = document.getElementById('tabCodigo')!;
    const tabEmail = document.getElementById('tabEmail')!;
    const panelCodigo = document.getElementById('panelCodigo')!;
    const panelEmail = document.getElementById('panelEmail')!;

    tabCodigo.addEventListener('click', () => {
      tabCodigo.className = 'flex-1 px-4 py-3 text-sm font-medium text-blue-700 border-b-2 border-blue-700 transition-base';
      tabEmail.className = 'flex-1 px-4 py-3 text-sm font-medium text-gray-500 hover:text-gray-700 transition-base';
      panelCodigo.classList.remove('hidden');
      panelEmail.classList.add('hidden');
    });

    tabEmail.addEventListener('click', () => {
      tabEmail.className = 'flex-1 px-4 py-3 text-sm font-medium text-blue-700 border-b-2 border-blue-700 transition-base';
      tabCodigo.className = 'flex-1 px-4 py-3 text-sm font-medium text-gray-500 hover:text-gray-700 transition-base';
      panelEmail.classList.remove('hidden');
      panelCodigo.classList.add('hidden');
    });

    // Cargar roles dinámicamente desde auth.ts
    const rolSelector = document.getElementById('rolSelector') as HTMLSelectElement;
    fetchRolesFromAuth().then((roles: Array<{ id: string; name: string }>) => {
      rolSelector.innerHTML = '<option value="">Seleccionar rol...</option>';
      roles.forEach((r: { id: string; name: string }) => {
        const opt = document.createElement('option');
        opt.value = r.id;
        opt.textContent = r.name;
        rolSelector.appendChild(opt);
      });
    });

    // Generar código usando auth.ts
    const generarBtn = document.getElementById('generarCodigoBtn')!;
    const codigoGenerado = document.getElementById('codigoGenerado')!;
    const codigoTexto = document.getElementById('codigoTexto')!;
    const codigoError = document.getElementById('codigoError')!;
    const copiarBtn = document.getElementById('copiarCodigoBtn')!;
    const usosMaximos = document.getElementById('usosMaximos') as HTMLInputElement;

    generarBtn.addEventListener('click', async () => {
      const roleId = rolSelector.value;
      if (!roleId) {
        codigoError.textContent = 'Seleccioná un rol para el código.';
        codigoError.classList.remove('hidden');
        codigoGenerado.classList.add('hidden');
        return;
      }

      codigoError.classList.add('hidden');
      codigoGenerado.classList.add('hidden');
      generarBtn.textContent = 'Generando...';
      (generarBtn as HTMLButtonElement).disabled = true;

      try {
        const estokId = getEstokActivoId();
        if (!estokId) throw new Error('No hay Estok activo');

        const usos = parseInt(usosMaximos.value, 10);
        const opciones = usos > 0 ? { usos_maximos: usos } : undefined;

        const data = await generarCodigoInvitacion(estokId, roleId, opciones);
        codigoTexto.textContent = data.codigo;
        codigoGenerado.classList.remove('hidden');
        codigoError.classList.add('hidden');
      } catch (err: any) {
        codigoError.textContent = err.message || 'Error al generar código';
        codigoError.classList.remove('hidden');
        codigoGenerado.classList.add('hidden');
      } finally {
        generarBtn.textContent = 'Generar código';
        (generarBtn as HTMLButtonElement).disabled = false;
      }
    });

    // Copiar al portapapeles
    copiarBtn.addEventListener('click', () => {
      const codigo = codigoTexto.textContent;
      if (codigo) {
        navigator.clipboard.writeText(codigo).then(() => {
          copiarBtn.textContent = '✅ Copiado';
          setTimeout(() => { copiarBtn.textContent = 'Copiar'; }, 2000);
        }).catch(() => {
          // Fallback: seleccionar texto
          const range = document.createRange();
          range.selectNodeContents(codigoTexto);
          const sel = window.getSelection();
          sel?.removeAllRanges();
          sel?.addRange(range);
        });
      }
    });

    // Cerrar modal
    const cerrarBtn = document.getElementById('cerrarModalBtn')!;
    const cerrar = () => overlay.remove();
    cerrarBtn.addEventListener('click', cerrar);
    overlay.addEventListener('click', (e) => {
      if (e.target === overlay) cerrar();
    });
  }

  invitarBtn?.addEventListener('click', crearModalInvitacion);

  // ===========================================================================
  // DARK MODE TOGGLE
  // ===========================================================================

  const darkModeToggle = document.getElementById('darkModeToggle')!;
  const sunIcon = document.getElementById('sunIcon')!;
  const moonIcon = document.getElementById('moonIcon')!;

  function setDarkMode(enabled: boolean) {
    if (enabled) {
      document.documentElement.classList.add('dark');
      localStorage.setItem('estok_dark_mode', 'true');
      sunIcon?.classList.remove('hidden');
      moonIcon?.classList.add('hidden');
    } else {
      document.documentElement.classList.remove('dark');
      localStorage.setItem('estok_dark_mode', 'false');
      sunIcon?.classList.add('hidden');
      moonIcon?.classList.remove('hidden');
    }
  }

  // Inicializar desde localStorage o preferencia del sistema
  const savedDarkMode = localStorage.getItem('estok_dark_mode');
  if (savedDarkMode === 'true') {
    setDarkMode(true);
  } else if (savedDarkMode === null && window.matchMedia('(prefers-color-scheme: dark)').matches) {
    setDarkMode(true);
  }

  darkModeToggle?.addEventListener('click', () => {
    const isDark = document.documentElement.classList.contains('dark');
    setDarkMode(!isDark);
  });

  // ===========================================================================
  // PWA: SERVICE WORKER + DETECCIÓN DE NUEVA VERSIÓN
  // ===========================================================================

  if ('serviceWorker' in navigator) {
    // Registrar el Service Worker
    navigator.serviceWorker.register('/sw.js').then((registration) => {
      console.log('✅ SW registrado:', registration.scope);

      // Detectar cuando hay un nuevo SW esperando (nuevo deploy)
      registration.addEventListener('updatefound', () => {
        const newWorker = registration.installing;
        if (newWorker) {
          newWorker.addEventListener('statechange', () => {
            if (newWorker.state === 'installed' && navigator.serviceWorker.controller) {
              // Hay una nueva versión disponible
              mostrarBannerActualizacion(registration);
            }
          });
        }
      });

      // Verificar si ya hay un SW esperando (por si la página se cargó mientras se actualizaba)
      if (registration.waiting && navigator.serviceWorker.controller) {
        mostrarBannerActualizacion(registration);
      }
    }).catch((err) => {
      console.warn('❌ Error registrando SW:', err);
    });

    // Escuchar mensajes del SW (como NEW_VERSION_AVAILABLE)
    navigator.serviceWorker.addEventListener('message', (event) => {
      if (event.data?.type === 'NEW_VERSION_AVAILABLE') {
        // Ya detectado por updatefound, pero por si acaso
      }
    });
  }

  // ===========================================================================
  // BANNER DE ACTUALIZACIÓN DISPONIBLE
  // ===========================================================================

  function mostrarBannerActualizacion(registration: ServiceWorkerRegistration) {
    // Evitar duplicados
    if (document.getElementById('updateBanner')) return;

    const banner = document.createElement('div');
    banner.id = 'updateBanner';
    banner.className = 'fixed bottom-0 left-0 right-0 z-[9999] bg-blue-700 text-white px-4 py-3 shadow-2xl transform translate-y-full transition-transform duration-500 ease-out';
    banner.innerHTML = `
      <div class="max-w-7xl mx-auto flex items-center justify-between gap-4">
        <div class="flex items-center gap-3">
          <svg class="w-6 h-6 flex-shrink-0 animate-pulse" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
          </svg>
          <div>
            <p class="text-sm font-semibold">🆕 Nueva versión disponible</p>
            <p class="text-xs text-blue-200">Hay una actualización de Estok lista para instalar.</p>
          </div>
        </div>
        <div class="flex items-center gap-2 flex-shrink-0">
          <button id="updateLaterBtn" class="text-xs text-blue-200 hover:text-white px-3 py-1.5 rounded-lg transition-base">
            Ahora no
          </button>
          <button id="updateNowBtn" class="text-xs font-semibold bg-white text-blue-700 px-4 py-1.5 rounded-lg hover:bg-blue-50 transition-base shadow-sm">
            🔄 Actualizar ahora
          </button>
        </div>
      </div>
    `;

    document.body.appendChild(banner);

    // Animar entrada
    requestAnimationFrame(() => {
      banner.classList.remove('translate-y-full');
    });

    // Botón "Actualizar ahora"
    document.getElementById('updateNowBtn')?.addEventListener('click', () => {
      // Enviar mensaje al SW para que skipWaiting()
      if (registration.waiting) {
        registration.waiting.postMessage({ type: 'SKIP_WAITING' });
      }
      // Recargar la página para usar la nueva versión
      window.location.reload();
    });

    // Botón "Ahora no"
    document.getElementById('updateLaterBtn')?.addEventListener('click', () => {
      banner.classList.add('translate-y-full');
      setTimeout(() => banner.remove(), 500);
    });
  }

  // ===========================================================================
  // PWA: BOTÓN DE INSTALACIÓN EN EL NAVBAR
  // ===========================================================================

  const installAppBtn = document.getElementById('installAppBtn') as HTMLButtonElement;

  // Si ya está instalada (display-mode: standalone), ocultar botón
  if (window.matchMedia('(display-mode: standalone)').matches) {
    installAppBtn?.classList.add('hidden');
  } else {
    // Mostrar botón siempre (no esperar beforeinstallprompt)
    installAppBtn?.classList.remove('hidden');
  }

  // Ocultar botón cuando se instale
  window.addEventListener('estok-pwa-installed', () => {
    installAppBtn?.classList.add('hidden');
  });

  // Click para instalar (usa la función global expuesta desde el dashboard)
  installAppBtn?.addEventListener('click', () => {
    (window as any).installPWA?.();
  });

  // ===========================================================================
  // CHAT: BOTÓN EN LA NAVBAR
  // ===========================================================================

  const chatNavBtn = document.getElementById('chatNavBtn')!;
  const chatNavBadge = document.getElementById('chatNavBadge')!;

  // Abrir el chat flotante al hacer click en el botón de la navbar
  chatNavBtn?.addEventListener('click', () => {
    // Disparar evento para que ChatModal lo escuche
    window.dispatchEvent(new CustomEvent('open-chat'));
  });

  // Escuchar actualizaciones del badge de no leídos
  window.addEventListener('chat-unread-update', ((e: CustomEvent) => {
    const count = e.detail?.count || 0;
    if (count > 0) {
      chatNavBadge.classList.remove('hidden');
    } else {
      chatNavBadge.classList.add('hidden');
    }
  }) as EventListener);
</script>
